# External Integrations

**Analysis Date:** 2026-08-15

## APIs & External Services
- **GitHub / Microsoft:** Mentioned in the system as a supported license/vendor, though no direct API SDK is explicitly integrated (mocked or internal mapping).
- **Search API:** MeiliSearch. Integrated via HTTP (`fetch` requests to `MEILISEARCH_HOST` at port 7700).

## Data Storage
### Databases
- **Relational DB:** PostgreSQL 17. Managed via Prisma ORM using `@prisma/adapter-pg`. 
- **Database Connection:** Connected via `DATABASE_URL` (typically `postgresql://...`).

### Caching & Queues
- **Cache / Message Broker:** Redis 8. Integrated via `ioredis` and `BullMQ` for caching and background job processing.
- **Connection:** Connected via `REDIS_URL`.

### File Storage
- **Object Storage:** SeaweedFS (S3-compatible). Used as an S3 Gateway for application file storage.
- **Connection Details:** Integrated via `S3_ENDPOINT` (port 8333), requiring `S3_ACCESS_KEY` and `S3_SECRET_KEY`.

## Authentication & Identity
- **Strategy:** JWT-based authentication using Passport (`passport-jwt`).
- **Secrets:** Configured via `JWT_SECRET` and `JWT_REFRESH_SECRET` environment variables.
- **Password Hashing:** `bcrypt` (v6).
- **Session Expiration:** Managed through environment variables (`JWT_ACCESS_EXPIRATION`, `JWT_REFRESH_EXPIRATION`).

## Monitoring & Observability
- **Logging:** Structured logging implemented using `pino` and `pino-http` in the NestJS backend.
- **Health Checks:** Basic Docker health checks exist for Postgres (`pg_isready`), Redis (`redis-cli ping`), and MeiliSearch (`curl /health`).

## CI/CD & Deployment
- **Containerization:** Comprehensive Docker Compose setup (`docker-compose.yml`, `docker-compose.dev.yml`).
- **Web Server Proxy:** Nginx (used for the frontend container proxying requests).

## Environment Configuration
Managed locally via `.env`. Key groups:
- Application Layer (`APP_PORT`, `NODE_ENV`)
- Database (`DATABASE_HOST`, `DATABASE_PASSWORD`, etc.)
- Redis Config
- JWT Secrets
- MeiliSearch (`MEILISEARCH_API_KEY`)
- SeaweedFS / S3 (`S3_BUCKET`, etc.)
- Frontend config (`VITE_API_URL`)

## Webhooks & Callbacks
- No external webhook listeners or callbacks (e.g., Stripe, SendGrid) are explicitly defined or implemented in the current API architecture. The application is entirely self-contained with its dependencies (Postgres, Redis, MeiliSearch, SeaweedFS).

*Generated by GSD codebase mapper. Last analysis: 2026-08-15.*
<!-- refreshed: 2026-08-15 -->
