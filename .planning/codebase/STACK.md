# Tech Stack & Configuration

**Analysis Date:** 2026-08-15

## Languages
- **Primary:** TypeScript (v5.9 for backend, v7.0 for frontend)
- **Secondary:** JavaScript (ESNext via Vite/V8)

## Runtime & Package Manager
- **Runtime:** Node.js `>=22.0.0`
- **Package Manager:** `pnpm` (`11.21.0`)
- **Monorepo Manager:** Turborepo (`^2.10.9`)

## Frameworks
### Core
- **Backend:** NestJS (v11)
- **Frontend:** React (v19) via Vite (v8)
- **ORM:** Prisma (v7.9.1) with `@prisma/adapter-pg`

### Testing
- **E2E Testing:** Playwright (v1.50)
- **Unit/Integration Testing:** Vitest (v4.1)
- **DOM Environment:** happy-dom

### Build
- **Build System:** Turborepo for workspace orchestration
- **Frontend Build:** Vite (v8), TypeScript Compiler (`tsc`)
- **Backend Build:** Nest CLI

## Key Dependencies
### Critical
- **State Management:** Zustand (v5)
- **Data Fetching:** React Query (v5.101), Axios
- **UI Components:** Ant Design (v6.6), `@ant-design/pro-components`
- **Routing:** React Router (v8)
- **Validation:** Zod (v3.25), `class-validator`

### Infrastructure
- **Message Queue / Jobs:** BullMQ (v6) with `@nestjs/bullmq`
- **Cache / Redis:** `ioredis` (v6)
- **Logging:** Pino (`pino-http`)
- **Security:** Helmet, bcrypt, Passport (JWT strategy)
- **WebSockets:** `@nestjs/platform-socket.io`

## Configuration
### Environment
- Configured via `.env` files (e.g., `.env.example`).
- **Variables managed:** Database URLs, Redis URIs, JWT secrets, MeiliSearch API keys, S3 Gateway credentials (SeaweedFS), Frontend API URLs.

### Build
- **Linting & Formatting:** Biome (v2.5) at the root, ESLint (v10) for workspace packages (`@uims/eslint-config`).
- **Workspace:** Defined in `pnpm-workspace.yaml`, split into `apps/api`, `apps/web`, and shared packages (`packages/shared-types`, etc.).
- **Docker:** Uses `docker-compose.yml` for full stack deployment and `docker-compose.dev.yml` for local overrides.

## Platform Requirements
- **OS:** Linux-based OS (Alpine in Docker images)
- **Node Environment:** >=22.0.0
- **Docker:** Required for running the database, Redis, MeiliSearch, and SeaweedFS services.

*Generated by GSD codebase mapper. Last analysis: 2026-08-15.*
<!-- refreshed: 2026-08-15 -->
